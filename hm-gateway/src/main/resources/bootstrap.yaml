spring:
  application:
    name: gateway # 服务名称
  cloud:
    nacos:
      server-addr: 192.168.174.136 # nacos地址
      config:
        file-extension: yaml # 文件后缀名
        shared-configs: # 共享配置
          - data-id: shared-log.yaml # 共享日志配置
